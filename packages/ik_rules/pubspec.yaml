name: ik_rules
description: Pure Dart port of the Idle Kingdoms game rules. No IO, no Flutter.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  ik_parity: ^0.1.0
  lints: ^6.0.0
  test: ^1.25.0
